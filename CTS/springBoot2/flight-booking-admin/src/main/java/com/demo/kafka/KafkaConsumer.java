package com.demo.kafka;

import org.springframework.stereotype.Service;

@Service
public class KafkaConsumer {

	/*
	 * @KafkaListener(topics = "my_topic", groupId = "my_group_id") public void
	 * getMessage(String msg) { System.out.println(msg); }
	 */
}
